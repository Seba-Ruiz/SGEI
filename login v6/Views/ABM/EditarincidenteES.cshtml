@model Model.incidente_escaner
@{
    ViewBag.Title = "Editar incidente de Escaner";
}
<ol class="breadcrumb">
    <li><a href="~/Inicio/index">Inicio</a></li>
    <li class="active">@(Model.id_incidente_escaner > 0 ? Model.descripcion : "Editar Ubicacion") </li>
</ol>
@using (Html.BeginForm("GuardarmodeloES", "ABM", FormMethod.Post))
{
    @Html.HiddenFor(x => x.id_incidente_escaner)
    <div class="row">
        <div class="col-sm-3"></div>
        <div class="col-sm-6">
            <div class="card-box">
                <h4 class="m-t-0 m-b-20 header-title"><b>EDITAR INCIDENTE DE ESCANER</b></h4>
                <div class="row">
                    <div class="col-md-12">
                        <h5><b>Edita las propiedades del incidente </b></h5>
                        <p class="text-muted m-b-30 font-13">
                            Edite el incidente
                        </p>
                        <div class="form-group">
                            @Html.Label("Ingrese Incidente")
                            @Html.TextBoxFor(x => x.descripcion, new { @class = "form-control", @style = "width:200px;", @required = "required" })
                            @Html.ValidationMessageFor(x => x.descripcion, null, new { @class = "label label-danger" })
                        </div>

                    </div>
                </div>
            </div>
        </div>
    </div>

    <div class="text-center">
        <button type="submit" class="btn-custom btn-inverse">Guardar</button>
    </div>

}
