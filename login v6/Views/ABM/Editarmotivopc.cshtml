@model Model.motivobajaPC

@{
    ViewBag.Title = "Editar Motivo de Baja de PC";
}

<ol class="breadcrumb">
    <li><a href="~/Inicio/index">Inicio</a></li>
    <li class="active">@(Model.id_motivobaja > 0 ? Model.descripcion : "Editar Motivo de Baja PC") </li>
</ol>

@using (Html.BeginForm("Guardarmotivopc", "ABM", FormMethod.Post))
{
    @Html.HiddenFor(x => x.id_motivobaja)
    <div class="row">
        <div class="col-sm-3"></div>
        <div class="col-sm-6">
            <div class="card-box">
                <h4 class="m-t-0 m-b-20 header-title"><b>EDITAR MOTIVO DE BAJA DE PC</b></h4>

                <div class="row">
                    <div class="col-md-12">
                        <h5><b>Edita las propiedades del Motivo de Baja </b></h5>
                        <p class="text-muted m-b-30 font-13">
                            Edite el motivo.
                        </p>
                        <div class="form-group">
                            @Html.Label("Ingrese Motivo de Baja de PC")
                            @Html.TextBoxFor(x => x.descripcion, new { @class = "form-control", @style = "width:200px;", @required = "required" })
                            @Html.ValidationMessageFor(x => x.descripcion, null, new { @class = "label label-danger" })
                        </div>
                        
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div class="text-center">
        <button type="submit" class="btn-custom btn-inverse">Guardar</button>
    </div>

}
